const enum PATHS {
  LANDING_PAGE = "/",
  LOGIN = "/login",
  FORGOT_PASSWORD = "/forgot-password",
  SIGNUP = "/signup",
  HOME = "/home",
  PROFILE = "/profile",
  LOGOUT = "/logout"
}

export default PATHS;
